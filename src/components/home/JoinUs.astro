<section id="join-us">
  <div class="join-us-body">
    <div class="image-wrapper">
      <img src="/home/join-us.svg" alt="Join us illustration" />
    </div>
    <div class="content-wrapper">
      <div class="join-content">
        <h1>Pridajte sa k nam</h1>
        <p>
          Vytvorili sme skupinu na sociálnej sieti Facebook, kde sa môžte pridať
          a byť tak v kontakte s ostatnými priaznivcami našej asociácie.
        </p>
        <span>Nazov: SASZ</span>
        <br />
        <a
          href="https://www.facebook.com/groups/2453636744795347/"
          target="_blank"
          class="fb-link">Pridať sa do skupiny</a
        >
      </div>
    </div>
  </div>
</section>

<style>
  #join-us {
    display: flex;
    justify-content: center;
    align-items: center;
    padding: clamp(3rem, 8vw, 10rem);
    width: 100%;
  }

  .join-us-body {
    display: flex;
    align-items: center;
    gap: clamp(2rem, 4vw, 4rem);
    width: 100%;
    max-width: 75rem;
  }

  .image-wrapper {
    width: 50%;
    display: flex;
    justify-content: center;
  }

  .image-wrapper img {
    width: 100%;
    max-width: 33rem;
    height: auto;
  }

  .content-wrapper {
    display: flex;
    flex-direction: column;
    width: 50%;
  }

  .join-content {
    max-width: 28rem;
  }

  .join-content h1 {
    font-size: clamp(2rem, 4vw, 2.5rem);
    color: var(--accent-dark);
    margin-bottom: 1rem;
  }

  .join-content p {
    font-size: clamp(1rem, 1.2vw, 1.2rem);
    line-height: 1.6;
    margin-bottom: 1rem;
    color: var(--foreground-light-sec);
  }

  .join-content span {
    font-size: clamp(1rem, 1.2vw, 1.2rem);
    font-weight: 500;
  }

  .fb-link {
    display: inline-block;
    margin-top: 1rem;
    padding: 0.8rem 1.5rem;
    background-color: var(--accent-dark);
    color: white;
    text-decoration: none;
    border-radius: 0.5rem;
    font-weight: 500;
    transition: background-color 0.3s ease;
  }

  .fb-link:hover {
    background-color: var(--accent-light);
  }

  @media screen and (max-width: 768px) {
    .join-us-body {
      flex-direction: column;
    }

    .image-wrapper,
    .content-wrapper {
      width: 100%;
    }

    .image-wrapper {
      max-width: 25rem;
    }

    .join-content {
      text-align: center;
      max-width: 100%;
    }
  }

  @media screen and (max-width: 480px) {
    #join-us {
      padding: 2rem 1rem;
    }

    .image-wrapper {
      max-width: 18rem;
    }
  }
</style>
