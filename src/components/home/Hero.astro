<section id="hero">
  <div class="hero-body">
    <div class="welcome-wrapper">
      <div class="img-wrapper">
        <!-- <div class="circle"></div> -->
        <img class="hero-img" src="/home/hero-image.svg" alt="hero-logo" />
      </div>
      <div class="welcome-content">
        <h1 class="hero-title">
          Slovenská <br /> Asociácia <br /> Štítnej žľazy
        </h1>
        <h1 class="hero-title-small">Slovenská Asociácia Štítnej žľazy</h1>
        <p>
          Sme novovzniknutá platforma pre ľudí s ochorením štítnej žľazy,
          rovnako aj pre ich blízkych. Našim cieľom je prepojiť komunitu
          pacientov s ochorením štítnej žľazy a odbornú obec, tak aby sme
          spoločne mohli hľadať riešenia, ktoré nám pomôžu zvýšiť kvalitu našich
          životov. Myslíme aj na novodiagnostikovaných pacientov, pre ktorých
          chceme vytvoriť jednoduchší štart do tohto životného štýlu.
        </p>
      </div>
    </div>
  </div>
</section>

<style>
  .hero-title-small {
    display: none;
  }

  #hero {
    min-height: calc(100svh - 5rem);
    width: 100%;
    padding: 2rem 0;
  }

  .hero-body {
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
    width: 100%;
    height: 100%;
  }

  .welcome-wrapper {
    display: flex;
    flex-direction: row;
    align-items: center;
    justify-content: center;
    gap: var(--spacing-lg);
    width: 100%;
    padding: var(--spacing-md);
  }

  .welcome-content {
    display: flex;
    width: 50%;
    flex-direction: column;
    text-align: left;
    justify-content: center;
  }

  .welcome-content p,
  .welcome-content h1 {
    max-width: 29rem;
  }

  .welcome-content p {
    color: var(--foreground-light-sec);
    font-size: var(--text-base);
    line-height: 1.6;
  }

  .hero-title {
    font-size: var(--text-2xl);
    font-weight: 700;
    text-align: left;
    color: var(--accent-dark);
    margin-bottom: var(--spacing-md);
    line-height: 1.2;
  }

  .img-wrapper {
    width: 50%;
    display: flex;
    flex-direction: column;
    justify-content: center;
    align-items: center;
  }

  .hero-img {
    width: 100%;
    max-width: 33rem;
    height: auto;
  }

  @media screen and (max-width: 940px) {
    .welcome-wrapper {
      flex-direction: column-reverse;
      gap: 2rem;
      padding: 1rem;
    }

    .hero-title {
      display: none;
    }

    .hero-title-small {
      display: block;
      font-size: clamp(1.8rem, 4vw, 3rem);
      font-weight: 700;
      text-align: left;
      color: var(--accent-dark);
      margin-bottom: 1rem;
      line-height: 1.3;
    }

    .welcome-content {
      width: 100%;
      text-align: left;
      align-items: flex-start;
    }

    .welcome-content p {
      max-width: 90%;
      text-align: left;
    }

    .img-wrapper {
      width: 100%;
      max-width: 25rem;
      padding: 0;
    }

    .hero-img {
      max-width: 100%;
    }
  }

  @media screen and (max-width: 480px) {
    #hero {
      padding: 1rem 0;
    }

    .welcome-wrapper {
      gap: 1.5rem;
    }

    .welcome-content p {
      font-size: 1rem;
      max-width: 100%;
    }

    .img-wrapper {
      max-width: 18rem;
    }
  }
</style>
